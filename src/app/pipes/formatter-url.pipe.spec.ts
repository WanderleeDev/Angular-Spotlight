import { FormatterUrlPipe } from './formatter-url.pipe';

describe('FormatterUrlPipe', () => {
  it('create an instance', () => {
    const pipe = new FormatterUrlPipe();
    expect(pipe).toBeTruthy();
  });
});
